# Ninja Saga — Game

Monorepo com **client** (OTCv8) e **server** (TFS 1.4.2).

Documentação do ecossistema, specs para agentes e skills Cursor:

**→ [`agents/README.md`](agents/README.md)**

Estrutura rápida:

| Path | Conteúdo |
|------|----------|
| [`agents/`](agents/) | README ecossistema, `.agents/skills/`, `.cursor/skills/` |
| [`client/`](client/) | Cliente OTCv8 — `client/.agents/` specs |
| [`server/`](server/) | Servidor TFS — `server/.agents/` specs |
